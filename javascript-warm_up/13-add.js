// Define a function called "add" that takes two integers as parameters and returns their sum.
function add(x, y) {
    return x + y;
}

// Now you can call the "add" function from anywhere in your code.
const result = add(4, 85);
console.log(result); // This should print 89 to the console
